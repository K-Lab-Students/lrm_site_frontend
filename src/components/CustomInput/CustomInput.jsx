import React from 'react'

import styles from './CustomInputStyle.module.css'

const CustomInput = ({title, defValue}) => {
    return (
        <div>CustomInput</div>
    )
}

export default CustomInput